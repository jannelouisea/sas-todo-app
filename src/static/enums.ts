export enum Sort {
    Incmpl1st = 'INCMPL_1ST',
    Cmpl1st = 'CMPL_1ST',
    DateAsc = 'DATE_ASC',
    DateDesc = 'DATE_DESC',
    TextAsc = 'TEXT_ASC',
    TextDesc = 'TEXT_DESC',
}

export enum MUIColor {
    Primary = 'primary',
    Success = 'success',
    Warning = 'warning',
    Neutral = 'neutral',
    Danger = 'danger',
}

export enum MUIIconColor {
    Inherit = 'inherit',
}

export enum MUIIconSize {
    Inherit = 'inherit',
}

export enum MUIVariant {
    Solid = 'solid',
    Soft = 'soft',
    Outlined = 'outlined',
    Plain = 'plain',
}

export enum MUISize {
    Small = 'sm',
    Medium = 'md',
    Large = 'lg',
}

export enum MUIPos {
    Right = 'right',
}

export enum MUIText {
    Label = 'body3'
}

export enum MUIFontSize {
    Small = 'small'
}